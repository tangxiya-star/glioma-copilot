# Screenshots for the README

Capture these three with the app running (`npm run dev`, backend warm), then save
them here with these exact filenames so the README image slots render:

| File | What to capture |
|---|---|
| `01-patient-panel.png` | The **Patient panel** view — the roster of 8 de-identified TCGA cases with provenance links. |
| `02-fit-and-audit.png` | The **Clinician review** view showing the per-criterion fit table **and** the independent-audit concordance panel (run audit on case-004 first). |
| `03-shared-decision-handout.png` | The **Shared decision** view with a generated handout (preference-ranked options + plain-language explanation + FAQ). |

Tip: use a clean browser window, light or dark theme consistently, and case-004 as the
subject so the screenshots line up with the walkthrough in the README.
